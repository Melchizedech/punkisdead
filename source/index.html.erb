---
pageable: true
per_page: 3
title: Punk is Dead Association
---

<!-- Body -->
<% if paginate && num_pages > 1 %>
  <% if prev_page %>
    <p><%= link_to 'Page précédente', prev_page %></p>
  <% end %>
<% end %>

<%= summary_for_list_of page_articles %>

<% if paginate %>
  <% if next_page %>
    <p><%= link_to 'Page suivante', next_page %></p>
  <% end %>
<% end %>
<!-- End of Body -->
